---
title: "Codebook"
format: html
editor: visual
jupyter: python3
---

## Imports

```{r}
library(tidyverse)
library(readr) 
library(tidymodels)
application_test<-read_csv(here::here("data/application_test.csv")) 
application_train <- read_csv(here::here("data/application_train.csv")) 
bureau_bal <- read_csv(here::here("data/bureau_balance.csv"))
bureau <- read_csv(here::here("data/bureau.csv")) 
ccbal <- read_csv(here::here("data/credit_card_balance.csv")) 
homecredit <- read_csv(here::here("data/HomeCredit_columns_description.csv")) 
cashbal <- read_csv(here::here("data/POS_CASH_balance.csv")) 
prevapp <- read_csv(here::here("data/previous_application.csv")) 
sample_sub <- read_csv(here::here("data/sample_submission.csv"))
```

```{r}
install.packages("LiblineaR")
```

```{r}
install.packages("discrim")
library(discrim)
```

```{r}
library(MASS)
```

## Reading in the Data

### Random Split of Data

```{r}
set.seed(123)
train_size <- floor(0.80 * nrow(application_train))
train_indices <- sample(seq_len(nrow(application_train)), size = train_size)
ran_train_data <- application_train[train_indices, ]
ran_val_data <- application_train[-train_indices, ]
```

### Stratified Split

```{r}
split_strat <- initial_split(application_train, prop = 0.8, strata = "TARGET")
strat_train_data <- training(split_strat)
strat_val_data <- testing(split_strat)
```

### Non Random Split - Represent Test Data

```{r}
glimpse(application_test)
```

## Data Engineering

```{r}

```

## Metric Custom Functions

### ROC-AUC, Accuracy, F-1, Specificity

```{r}
accuracy <- function(y_hat, y){
    correct <- sum(y_hat == y)
    total <- length(y)
    return(correct / total)
}

precision <- function(y_hat, y){
    tp <- sum ((y_hat == 1) && (y ==1))
    fp <- sum ((y_hat == 1) && (y == 0))
    precision <- tp / (tp+fp)
    return(precision)
}

recall <- function(y_hat, y){
    tp <- sum ((y_hat == 1) && (y ==1))
    fn <- sum ((y_hat == 0) && (y == 1))
    recall <- tp / (tp+fn)
    return(recall)
}

f1 <- function(y_hat, y){
    precision <- precision(y_hat, y)
    recall <- recall(y_hat, y)
    return (2 * (precision * recall) / (precision + recall))
}

specificity <- function(y_hat, y){
    fp <- sum ((y_hat == 1) && (y == 0))
    tn <- sum ((y_hat == 0) && (y == 0))
    return (tn/(tn+fp))
}

roc_auc <- function(y, y_hat){
    thresholds <- seq(0, 1, by = 0.01)
    tpr <- c()
    fpr <- c()
    for (t in thresholds){
        tp <- sum((y_hat >= t) && (y == 1))
        fn <- sum((y_hat < t) && (y == 1))
        fp <- sum((y_hat >= t) && (y == 0))
        tn <- sum((y_hat < t) && (y == 0))
        tpr <- c(tpr, tp / (tp + fn))
        fpr <- c(fpr, fp / (fp + tn))
    }
    auc <- 0
    for (i in 2:length(tpr)){
        auc <- auc + (fpr[i] - fpr[i-1]) * (tpr[i] + tpr[i-1]) / 2
    }
    return(auc)
}

```

### Cross Validation Function

```{r}
cross_val <- function(model, data, k =5){
    set.seed(23)
    folds <- sample((rep(1:k, length.out = nrow(data))))
    metrics <- data.frame(Accuracy = numeric(k),
                          F1 = numeric(k),
                          Specificity = numeric(k),
                          ROC_AUC = numeric(k))
    for (i in 1:k){
        train_fold = data[folds != i, ]
        val_fold = data[folds == i,]

        model_fit(model, data = train_fold)
        preds <- predict(model, val_fold, type = "prob")
        y_hat <- ifelse(preds$.pred_1 >= 0.5, 1, 0)
        y <- val_fold$TARGET
        metrics$Accuracy[i] <- accuracy(y_hat, y)
        metrics$F1[i] <- f1(y_hat, y)
        metrics$Specificity[i] <- specificity(y_hat, y)
        metrics$ROC_AUC[i] <- roc_auc(y, preds$.pred_1)
        metrics$Accuracy[k+1] <- mean(metrics$Accuracy)
        metrics$F1[k+1] <- mean(metrics$F1)
        metrics$Specificity[k+1] <- mean(metrics$Specificity)
        metrics$ROC_AUC[k+1] <- mean(metrics$ROC_AUC)
    }
    return(metrics)
}
```

## Models

### Linear (Taras Part)

```{r}
#concatanate string for logistic regression here
#WORKS
generateModelFormula <- function(yResponse,xPredictors)
{
  formula <- as.formula(paste(yResponse,"~",paste(xPredictors,collapse="+")))
  return(formula)
}


```

```{r}
yResponse="TARGET"
xPredictors=c("AMT_INCOME_TOTAL","AMT_CREDIT","AMT_ANNUITY","AMT_GOODS_PRICE")
generateModelFormula(yPredictor,xPredictors)
```

```{r}
#we must factor the target variable in order to get the classification to work!!
strat_train_data$TARGET <- as.factor(strat_train_data$TARGET)
```

```{r}
ran_train_data$TARGET <- as.factor(ran_train_data$TARGET)
```

```{r}
ran_val_data$TARGET <- as.factor(ran_val_data$TARGET)
```

```{r}
#logistic regression model function here: we would pass in the training data sets for the data frame (strat_train_data, ran_train_data)!! 
#generates predictions from the training data sets! 



#add in testDataFrame once i obtain it!! 
#testDataFrame

logistic_regression_model_fit <- function(trainDataFrame,yResponse,xPredictors)
{
  #yResponse needs to be converted to a factor!! 
  #trainDataFrame$yResponse <- as.factor(trainDataFrame$yResponse)
  
  #comment this out for now
  formula <- generateModelFormula(yResponse,xPredictors)
  fitted_logistic_model <- logistic_reg() %>% 
    #set the engine: 
    set_engine("glm") %>% 
    #set the mode: 
    set_mode("classification")%>%
    #Fit the Model 
    fit(formula,data=trainDataFrame)
  tidy(fitted_logistic_model) #generates a summary table with the intercepts
  
  #generate test prediction for model evaluation: pass in trained model object, test data set, and type of prediction which is "class" 
  #pred_class <- predict(fitted_logistic_model,new_data=testDataFrame,type="class")
  #printing out the test prediction 
  #pred_class
  
  #generate predictions for class probabilities here: 
  #class_prob_prediction <- predict(fitted_logistic_model,new_data=testDataFrame,type="prob")
  
  #printing out probability of prediction for each class
  #class_prob_prediction
  
}

```

```{r}
#run logistic_regression_model_fit on strat_train_data
yResponse="TARGET"
logistic_regression_model_fit(strat_train_data,yResponse,xPredictors)

```

```{r}
#run logistic_regression_model_fit on ran_train_data
yResponse="TARGET"
logistic_regression_model_fit(ran_train_data,yResponse,xPredictors)
```

```{r}
#run logistic_regression_model_fit on validation train dataset
#run logistic_regression_model_fit on ran_val_data
yResponse="TARGET"
logistic_regression_model_fit(ran_val_data,yResponse,xPredictors)
```

```{r}
#support vector machine classification model here
#pass in testData once i have it!!

SVMClassification <- function(trainData,yReponse,xPredictors,costVal,marginVal)
{
  #costVal and marginVal is a hyperparameter here!! 
  svm_model <- svm_linear(cost=costVal,margin=marginVal) %>%
    set_mode("classification") %>% 
    set_engine("LiblineaR")
  
  #fit the classification model on train data!! 
  formula <- generateModelFormula(yResponse,xPredictors)
  formula
  svmModel_fit <- svm_model %>% fit(formula,data=trainData)
  svmModel_fit
  
  #uses train model fit to conduct prediction of response variable with the test data: 
  #predict(svmModel_fit,testData)
}
```

```{r}
#run SVMClassification on strat_train_data
costVal=0.1
marginVal=1
SVMClassification(strat_train_data,yResponse,xPredictors,costVal,marginVal)

```

```{r}
#run SVMClassification on ran_train_data
costVal=0.1
marginVal=1
SVMClassification(ran_train_data,yResponse,xPredictors,costVal,marginVal)
```

```{r}
#run SVMClassification on validation train dataset 
#run SVMClassification on val_train_data
costVal=0.1
marginVal=1
SVMClassification(ran_val_data,yResponse,xPredictors,costVal,marginVal)

```

```{r}
#RUN THIS ONE NEXT!! 

#LDA: Linear Discriminant Analysis Model function here

#dont have testData as of right now, so currently exclude 
#testData
LDA_model_fit <- function(trainData,yResponse,xPredictors)
{
  discrim_model <- discrim_linear(
    mode="classification",
    penalty=NULL,
    regularization_method=NULL,
    engine="MASS"
  )
  
  formula <- generateModelFormula(yResponse,xPredictors)
  discrim_modelFit <- discrim_model %>% fit(formula,data=trainData)
  
  discrim_modelFit
  
  #uncomment line below once i have the testData
  #predict(discrim_modelFit,testData)
  
}


```

```{r}
#run LDA on strat_train_data
LDA_model_fit(strat_train_data,yResponse,xPredictors)
```

```{r}
#run LDA on ran_train_data
LDA_model_fit(ran_train_data,yResponse,xPredictors)

```

```{r}
#running data on validation training data 
#run LDA on ran_val_data
LDA_model_fit(ran_val_data,yResponse,xPredictors)
```
